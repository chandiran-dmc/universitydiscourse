/**
 * This is a react component class for the create post button
 * inside the action bar on the main page
 * 
 * The user should be redirected to the create post page after
 * clicking on this button
 * 
 * Please look at the UI mockup for image explanation
 */

import React, { Component } from 'react'

export default class CreatePostButton extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
